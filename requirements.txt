#git+https://github.com/patrickuhlmann/juntagrico.git@main
juntagrico==2.0.9

gunicorn==26.2.0
psycopg==3.3.4
whitenoise==6.12.0
juntagrico-assignment-request==2.0.0
juntagrico-billing==2.0.0
juntagrico-pg==2.0.0

# juntagrico-billing 2.0.0 importiert requests (util/bexio_api.py), deklariert es in seinen
# Metadaten aber nicht (upstream erst nach dem Release nachgezogen). Ohne diesen Eintrag
# scheitert schon der Import von juntagrico_billing.urls und damit jeder manage.py-Aufruf.
requests==2.34.2

# transitive dependency (pyHanko <- xhtml2pdf), pinned so Dependabot gates upgrades via CI
tzlocal==5.4.4
